This project for starting API 
You need install bcrypt, body-parser, express, jsonwebtoken, mongoose, morgan, multer  

## Available Scripts
Add next script -> "start": "nodemon server.js"

In the project directory, you can run:

### `npm install`

### `npm start`
